---
// Author: Cascade (claude-sonnet-4-20250514)
// Date: 2026-02-23
// PURPOSE: Home page — "The Directed Graph" concept. Hero with manuscript background,
//          LODA split-pane, Toolbox origin screenshots, ARC showcase with real puzzle,
//          DAG throughline, and Lobster Playground linking to assistant tribute pages.
//          Uses ALL image assets in public/generated, public/images, public/simon.
// SRP/DRY check: Pass — reuses Base layout, self-contained page logic
import Base from '../layouts/Base.astro';
---
<Base title="Home">

  <!-- ════════════════════════════════════════════════════════════════════
       HERO — Voynich Manuscript background + DAG overlay + anchor quote
       ════════════════════════════════════════════════════════════════════ -->
  <section class="relative min-h-[94vh] flex items-center justify-center overflow-hidden">
    <!-- Manuscript background image — the "awakened page" -->
    <img
      src="/generated/hero-manuscript.png"
      alt=""
      aria-hidden="true"
      class="absolute inset-0 w-full h-full object-cover opacity-20 pointer-events-none"
    />
    <!-- Dark vignette overlay for text legibility -->
    <div class="absolute inset-0 bg-gradient-to-b from-bg-primary/60 via-bg-primary/30 to-bg-primary/80 pointer-events-none"></div>

    <!-- SVG DAG edges — subtle animated connections over the manuscript -->
    <svg class="dag-hero-svg absolute inset-0 w-full h-full pointer-events-none" viewBox="0 0 1200 800" preserveAspectRatio="xMidYMid slice" aria-hidden="true">
      <defs>
        <filter id="edge-glow">
          <feGaussianBlur stdDeviation="2.5" result="blur" />
          <feMerge><feMergeNode in="blur" /><feMergeNode in="SourceGraphic" /></feMerge>
        </filter>
      </defs>
      <g filter="url(#edge-glow)" opacity="0.18">
        <line x1="80" y1="120" x2="260" y2="200" stroke="#38bdf8" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" />
        <line x1="260" y1="200" x2="420" y2="150" stroke="#4ade80" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:0.2s" />
        <line x1="260" y1="200" x2="340" y2="340" stroke="#f97316" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:0.4s" />
        <line x1="520" y1="300" x2="680" y2="380" stroke="#4ade80" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:0.3s" />
        <line x1="680" y1="380" x2="820" y2="310" stroke="#38bdf8" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:0.6s" />
        <line x1="680" y1="380" x2="740" y2="520" stroke="#f97316" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:0.8s" />
        <line x1="900" y1="180" x2="1040" y2="270" stroke="#22d3ee" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:0.7s" />
        <line x1="1040" y1="270" x2="1120" y2="200" stroke="#38bdf8" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:1.0s" />
        <line x1="280" y1="560" x2="460" y2="620" stroke="#22d3ee" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:0.5s" />
        <line x1="460" y1="620" x2="640" y2="570" stroke="#4ade80" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:0.9s" />
        <line x1="640" y1="570" x2="840" y2="640" stroke="#f97316" stroke-width="1" stroke-dasharray="1000" class="animate-draw-edge" style="animation-delay:1.2s" />
      </g>
      <g opacity="0.25">
        <circle cx="80" cy="120" r="3" fill="#38bdf8" /><circle cx="260" cy="200" r="4.5" fill="#38bdf8" />
        <circle cx="420" cy="150" r="3" fill="#4ade80" /><circle cx="340" cy="340" r="3" fill="#f97316" />
        <circle cx="520" cy="300" r="3" fill="#4ade80" /><circle cx="680" cy="380" r="5" fill="#4ade80" />
        <circle cx="820" cy="310" r="3" fill="#38bdf8" /><circle cx="740" cy="520" r="3.5" fill="#f97316" />
        <circle cx="900" cy="180" r="3" fill="#22d3ee" /><circle cx="1040" cy="270" r="4" fill="#22d3ee" />
        <circle cx="1120" cy="200" r="3" fill="#38bdf8" /><circle cx="280" cy="560" r="3" fill="#22d3ee" />
        <circle cx="460" cy="620" r="4" fill="#4ade80" /><circle cx="640" cy="570" r="3" fill="#4ade80" />
        <circle cx="840" cy="640" r="4" fill="#f97316" />
      </g>
    </svg>

    <!-- Hero text content -->
    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 text-center">
      <p class="font-mono text-xs text-edge-green uppercase tracking-[0.25em] mb-6">
        VoynichLabs // Directed Acyclic Graphs
      </p>
      <h1 class="text-3xl sm:text-5xl lg:text-6xl font-bold tracking-tight text-text-primary mb-8 leading-[1.1]">
        Generative computation<br/>from minimal rules.
      </h1>

      <!-- Anchor Quote — the thesis -->
      <blockquote class="max-w-2xl mx-auto mb-10 border-l-2 border-edge-green/40 pl-6 text-left">
        <p class="text-lg sm:text-xl text-text-muted font-light leading-relaxed italic">
          "Compute the output from minimal rules.<br/>
          <span class="text-text-primary font-normal not-italic">Don't enumerate. Don't store. Generate.</span>"
        </p>
        <cite class="block mt-3 text-xs font-mono text-text-muted not-italic">
          -- Simon Strandgaard, on every project he has built
        </cite>
      </blockquote>

      <p class="text-sm text-text-muted max-w-xl mx-auto mb-10 leading-relaxed">
        From procedurally generating graphics to automatically discovering mathematical formulas,
        everything is a DAG of processing nodes. The domain changes. The architecture never does.
      </p>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
        <a href="/simon"
           class="px-6 py-2.5 bg-bg-surface border border-node-blue/40 hover:border-node-blue text-node-blue font-mono text-sm rounded transition-colors duration-200">
          explore ./simon
        </a>
        <a href="/projects"
           class="px-6 py-2.5 bg-bg-surface border border-border hover:border-border-active text-text-muted hover:text-text-primary font-mono text-sm rounded transition-colors duration-200">
          ls ./projects
        </a>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════════════════════════════════
       THE TOOLBOX ORIGIN — Where DAGs began (2003-2007)
       Real screenshots from Simon's procedural graphics application
       ════════════════════════════════════════════════════════════════════ -->
  <section class="border-t border-border py-20 sm:py-24">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="grid grid-cols-1 lg:grid-cols-5 gap-8 items-start">
        <!-- Text: 2 cols -->
        <div class="lg:col-span-2">
          <p class="font-mono text-xs text-edge-green uppercase tracking-[0.2em] mb-3">
            Origin // 2003-2007
          </p>
          <h2 class="text-2xl sm:text-3xl font-bold text-text-primary mb-4">
            Where DAGs began:<br/>The Toolbox
          </h2>
          <p class="text-sm text-text-muted leading-relaxed mb-4">
            Inspired by Farbrausch's <strong class="text-text-primary">werkzeug</strong> demoscene tool,
            Simon built <strong class="text-text-primary">Toolbox</strong> — a macOS application where
            you connect processing nodes into a graph and the machine generates procedural graphics.
          </p>
          <p class="text-sm text-text-muted leading-relaxed mb-4">
            Stripe nodes, Noise nodes, Bump mapping, color mixing — all wired together as a DAG.
            Set the parameters, hit render. 512x512 patterns generated from pure math. No textures stored.
          </p>
          <p class="font-mono text-xs text-text-muted mt-4">
            input &rarr; <span class="text-edge-green">[Stripe]</span> &rarr;
            <span class="text-edge-green">[Noise]</span> &rarr;
            <span class="text-edge-green">[Bump]</span> &rarr;
            <span class="text-edge-green">[Join]</span> &rarr; output
          </p>
          <a href="https://www.youtube.com/watch?v=Fn9_sVFLVMo" target="_blank" rel="noopener"
             class="inline-block mt-5 font-mono text-xs text-node-blue hover:text-text-primary transition-colors">
            Watch Toolbox in action -->
          </a>
        </div>

        <!-- Screenshots: 3 cols, 2x2 grid -->
        <div class="lg:col-span-3 grid grid-cols-2 gap-2">
          <div class="relative group overflow-hidden rounded border border-border">
            <img src="/simon/toolbox-1.jpg" alt="Toolbox: stripe and dot pattern compositor with node graph"
                 class="w-full aspect-video object-cover group-hover:scale-[1.03] transition-transform duration-300" loading="lazy" />
            <div class="absolute bottom-0 inset-x-0 bg-bg-primary/80 px-2 py-1">
              <p class="font-mono text-[9px] text-edge-green">Stripe + Dot + Noise compositing</p>
            </div>
          </div>
          <div class="relative group overflow-hidden rounded border border-border">
            <img src="/simon/toolbox-2.jpg" alt="Toolbox: procedural geometric shapes from connected nodes"
                 class="w-full aspect-video object-cover group-hover:scale-[1.03] transition-transform duration-300" loading="lazy" />
            <div class="absolute bottom-0 inset-x-0 bg-bg-primary/80 px-2 py-1">
              <p class="font-mono text-[9px] text-edge-green">Shape + Align + Transform DAG</p>
            </div>
          </div>
          <div class="relative group overflow-hidden rounded border border-border">
            <img src="/simon/toolbox-3.jpg" alt="Toolbox: node graph with Stripe, Rotate, Bump mapping nodes"
                 class="w-full aspect-video object-cover group-hover:scale-[1.03] transition-transform duration-300" loading="lazy" />
            <div class="absolute bottom-0 inset-x-0 bg-bg-primary/80 px-2 py-1">
              <p class="font-mono text-[9px] text-edge-green">Stripe > Rotate > Mix > Bump pipeline</p>
            </div>
          </div>
          <div class="relative group overflow-hidden rounded border border-border">
            <img src="/simon/toolbox-4.jpg" alt="Toolbox: 512x512 procedural stereogram output from bump-mapped noise"
                 class="w-full aspect-video object-cover group-hover:scale-[1.03] transition-transform duration-300" loading="lazy" />
            <div class="absolute bottom-0 inset-x-0 bg-bg-primary/80 px-2 py-1">
              <p class="font-mono text-[9px] text-edge-green">512x512 stereogram from Bump node</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════════════════════════════════
       LODA SHOWCASE — Split-pane: context + terminal
       With loda-mine.png as a visual accent
       ════════════════════════════════════════════════════════════════════ -->
  <section class="relative border-t border-border py-20 sm:py-28 overflow-hidden">
    <!-- Faint loda-mine background behind the whole section -->
    <img src="/generated/loda-mine.png" alt="" aria-hidden="true"
         class="absolute right-0 top-0 w-1/2 h-full object-cover opacity-[0.06] pointer-events-none" />

    <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6">
      <div class="mb-12">
        <p class="font-mono text-xs text-rust-orange uppercase tracking-[0.2em] mb-3">
          LODA // Automated Mathematical Discovery
        </p>
        <h2 class="text-2xl sm:text-3xl font-bold text-text-primary">
          Assembly-level precision. Machine-discovered formulas.
        </h2>
      </div>

      <!-- Split pane -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-0 border border-border rounded-lg overflow-hidden">
        <!-- Left pane: Context -->
        <div class="bg-bg-secondary p-6 sm:p-8 border-b lg:border-b-0 lg:border-r border-border">
          <p class="font-mono text-xs text-text-muted uppercase tracking-widest mb-4">Context</p>
          <h3 class="text-lg font-semibold text-text-primary mb-4">
            Narayana Numbers <span class="font-mono text-rust-orange text-sm">(A001263)</span>
          </h3>
          <p class="text-sm text-text-muted leading-relaxed mb-4">
            This program was <strong class="text-text-primary font-medium">automatically discovered</strong>
            by LODA's automated miner (<span class="font-mono text-edge-green text-xs">loader3229</span>)
            — not written by hand. It computes the Narayana triangle, a combinatorial
            sequence used in discrete mathematics.
          </p>
          <p class="text-sm text-text-muted leading-relaxed mb-6">
            The formula was <strong class="text-text-primary font-medium">derived automatically</strong>
            from the assembly execution. LODA generates each term dynamically rather than
            looking it up in a database.
          </p>

          <!-- Narayana triangle -->
          <div class="bg-bg-terminal border border-border rounded p-4 font-mono text-xs mb-6">
            <p class="text-text-muted mb-2">// Narayana triangle, rows 1-5:</p>
            <div class="text-edge-green space-y-0.5">
              <p>1</p><p>1 &nbsp;1</p><p>1 &nbsp;3 &nbsp;1</p><p>1 &nbsp;6 &nbsp;6 &nbsp;1</p><p>1 10 20 10 &nbsp;1</p>
            </div>
          </div>

          <div class="text-xs font-mono text-text-muted">
            <span class="text-rust-orange">T(n,k)</span> = C(n-1,k-1) * C(n,k-1) / k
          </div>
          <a href="https://oeis.org/A001263" target="_blank" rel="noopener"
             class="inline-block mt-5 font-mono text-xs text-node-blue hover:text-text-primary transition-colors">
            oeis.org/A001263 -->
          </a>
        </div>

        <!-- Right pane: Terminal -->
        <div class="bg-bg-terminal p-6 sm:p-8 relative">
          <div class="flex items-center gap-4 mb-5">
            <div class="flex gap-1.5">
              <div class="w-2.5 h-2.5 rounded-full bg-red-500/60"></div>
              <div class="w-2.5 h-2.5 rounded-full bg-warn-amber/60"></div>
              <div class="w-2.5 h-2.5 rounded-full bg-edge-green/60"></div>
            </div>
            <span class="font-mono text-[10px] text-text-muted">loda-rust // A001263.asm</span>
          </div>
          <pre class="font-mono text-xs leading-[1.8] overflow-x-auto"><code><span class="text-text-muted">; A001263: Triangle of Narayana numbers</span>
<span class="text-text-muted">; Submitted by loader3229</span>
<span class="text-text-muted">; 1,1,1,1,3,1,1,6,6,1,1,10,20,10,1...</span>

<span class="text-rust-orange">#offset</span> <span class="text-warn-amber">1</span>
<span class="text-node-blue">mov</span> <span class="text-edge-green">$1</span>,<span class="text-edge-green">$0</span>
<span class="text-node-blue">mul</span> <span class="text-edge-green">$1</span>,<span class="text-warn-amber">8</span>
<span class="text-node-blue">nrt</span> <span class="text-edge-green">$1</span>,<span class="text-warn-amber">2</span>         <span class="text-text-muted">; floor(sqrt(8n))</span>
<span class="text-node-blue">sub</span> <span class="text-edge-green">$1</span>,<span class="text-warn-amber">1</span>
<span class="text-node-blue">div</span> <span class="text-edge-green">$1</span>,<span class="text-warn-amber">2</span>         <span class="text-text-muted">; row index</span>
<span class="text-node-blue">mov</span> <span class="text-edge-green">$2</span>,<span class="text-edge-green">$1</span>
<span class="text-node-blue">add</span> <span class="text-edge-green">$2</span>,<span class="text-warn-amber">1</span>
<span class="text-node-blue">bin</span> <span class="text-edge-green">$2</span>,<span class="text-warn-amber">2</span>         <span class="text-text-muted">; C(row+1, 2)</span>
<span class="text-node-blue">sub</span> <span class="text-edge-green">$0</span>,<span class="text-edge-green">$2</span>
<span class="text-node-blue">sub</span> <span class="text-edge-green">$0</span>,<span class="text-warn-amber">1</span>         <span class="text-text-muted">; column index</span>
<span class="text-node-blue">mov</span> <span class="text-edge-green">$2</span>,<span class="text-edge-green">$1</span>
<span class="text-node-blue">add</span> <span class="text-edge-green">$2</span>,<span class="text-warn-amber">1</span>
<span class="text-node-blue">bin</span> <span class="text-edge-green">$2</span>,<span class="text-edge-green">$0</span>         <span class="text-text-muted">; C(row+1, k-1)</span>
<span class="text-node-blue">bin</span> <span class="text-edge-green">$1</span>,<span class="text-edge-green">$0</span>         <span class="text-text-muted">; C(row, k-1)</span>
<span class="text-node-blue">add</span> <span class="text-edge-green">$0</span>,<span class="text-warn-amber">1</span>         <span class="text-text-muted">; k</span>
<span class="text-node-blue">mul</span> <span class="text-edge-green">$1</span>,<span class="text-edge-green">$2</span>
<span class="text-node-blue">div</span> <span class="text-edge-green">$1</span>,<span class="text-edge-green">$0</span>         <span class="text-text-muted">; Narayana formula</span>
<span class="text-node-blue">mov</span> <span class="text-edge-green">$0</span>,<span class="text-edge-green">$1</span>         <span class="text-text-muted">; output</span></code></pre>
        </div>
      </div>

      <div class="mt-6 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
        <p class="text-xs text-text-muted max-w-lg">
          17 lines. No loops. LODA's evolutionary miner synthesized this from the OEIS definition.
          Simon built the Rust runtime (~6,000 commits) that makes it possible.
        </p>
        <a href="https://github.com/loda-lang/loda-rust" target="_blank" rel="noopener"
           class="font-mono text-xs text-rust-orange hover:text-text-primary transition-colors shrink-0">
          loda-lang/loda-rust -->
        </a>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════════════════════════════════
       ARC SHOWCASE — The hardest AI benchmark
       Uses arc-grid.png (glass tiles) + arc_puzzle_faa9f03d.gif (real puzzle)
       ════════════════════════════════════════════════════════════════════ -->
  <section class="border-t border-border bg-bg-secondary py-20 sm:py-24">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-10 items-center">
        <!-- Left: visuals stacked -->
        <div class="space-y-4">
          <!-- Glass-tile ARC grid — the signature image -->
          <div class="relative rounded-lg overflow-hidden border border-border">
            <img src="/generated/arc-grid.png"
                 alt="ARC puzzle grid rendered as physical glass tiles on dark surface, mid-transformation — left columns intact, right column shattering"
                 class="w-full object-cover" loading="lazy" />
            <div class="absolute top-3 left-3 bg-bg-primary/80 px-2 py-1 rounded">
              <p class="font-mono text-[9px] text-arc-cyan">ARC-AGI // Input &rarr; Output transformation</p>
            </div>
          </div>
          <!-- Actual faa9f03d puzzle gif -->
          <div class="flex gap-4 items-center bg-bg-terminal border border-border rounded-lg p-4">
            <img src="/images/arc_puzzle_faa9f03d.gif"
                 alt="ARC puzzle faa9f03d — Training 1 Input grid with blue, green, red cells"
                 class="w-28 h-28 object-contain rounded flex-shrink-0" loading="lazy" />
            <div>
              <p class="font-mono text-xs text-arc-cyan mb-1">task://faa9f03d</p>
              <p class="text-sm text-text-muted leading-relaxed">
                The infamously difficult task. Created by Simon. Try it yourself.
              </p>
              <a href="https://arc.markbarney.net/task/faa9f03d" target="_blank" rel="noopener"
                 class="font-mono text-xs text-node-blue hover:text-text-primary transition-colors mt-2 inline-block">
                arc.markbarney.net/task/faa9f03d -->
              </a>
            </div>
          </div>
        </div>

        <!-- Right: text + stats -->
        <div>
          <p class="font-mono text-xs text-arc-cyan uppercase tracking-[0.2em] mb-3">
            ARC-AGI // Abstraction and Reasoning
          </p>
          <h2 class="text-2xl sm:text-3xl font-bold text-text-primary mb-4">
            Dozens of tasks created.<br/>The hardest AI benchmark on earth.
          </h2>
          <p class="text-sm text-text-muted leading-relaxed mb-4">
            Simon didn't just study ARC — he helped build it in collaboration with dozens of contributors.
            He helped create the ARC2 tasks, built the browser-based solving tool, recorded human interaction traces,
            and curated the datasets the research community depends on.
          </p>

          <!-- ARC Prize recognition -->
          <div class="border-l-2 border-arc-cyan/40 pl-4 mb-6">
            <p class="text-sm text-text-primary leading-relaxed italic">
              "We thank Mark Barney and Simon Strandgaard for their ongoing efforts to build tools,
              answer questions, and be a resource to the community."
            </p>
            <cite class="block mt-2 text-xs font-mono text-text-muted not-italic">
              -- Mike Knoop, ARC Prize Foundation 2025
            </cite>
          </div>

          <!-- Stats row -->
          <div class="grid grid-cols-3 gap-3">
            <div class="bg-bg-terminal border border-border rounded p-3 text-center">
              <p class="text-xl font-bold font-mono text-arc-cyan">Dozens</p>
              <p class="text-[10px] text-text-muted mt-1">ARC2 tasks</p>
            </div>
            <div class="bg-bg-terminal border border-border rounded p-3 text-center">
              <p class="text-xl font-bold font-mono text-arc-cyan">725</p>
              <p class="text-[10px] text-text-muted mt-1">commits</p>
            </div>
            <div class="bg-bg-terminal border border-border rounded p-3 text-center">
              <p class="text-xl font-bold font-mono text-warn-amber">117</p>
              <p class="text-[10px] text-text-muted mt-1">dataset stars</p>
            </div>
          </div>

          <div class="flex gap-3 mt-5">
            <a href="https://github.com/neoneye/ARC-Interactive" target="_blank" rel="noopener"
               class="font-mono text-xs text-arc-cyan hover:text-text-primary transition-colors">
              ARC-Interactive -->
            </a>
            <a href="https://github.com/neoneye/arc-dataset-collection" target="_blank" rel="noopener"
               class="font-mono text-xs text-text-muted hover:text-text-primary transition-colors">
              arc-dataset-collection -->
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════════════════════════════════
       LODA-MINE — Full-width visual break
       ════════════════════════════════════════════════════════════════════ -->
  <section class="relative border-t border-border overflow-hidden">
    <div class="relative">
      <img src="/generated/loda-mine.png"
           alt="The Formula Mine — glowing wireframe hash symbol surrounded by floating mathematical symbols in a deep cave"
           class="w-full h-56 sm:h-72 lg:h-80 object-cover opacity-50" loading="lazy" />
      <div class="absolute inset-0 flex items-center justify-center bg-bg-primary/30">
        <div class="text-center px-4">
          <p class="font-mono text-xs text-rust-orange uppercase tracking-[0.3em] mb-3">
            The Formula Mine
          </p>
          <p class="text-lg sm:text-xl text-text-primary font-light">
            Automated discovery of mathematical truth from minimal rules.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════════════════════════════════
       DAG THROUGHLINE — Connecting all of Simon's work
       ════════════════════════════════════════════════════════════════════ -->
  <section class="border-t border-border py-20 sm:py-24">
    <div class="max-w-5xl mx-auto px-4 sm:px-6">
      <div class="mb-12">
        <p class="font-mono text-xs text-node-blue uppercase tracking-[0.2em] mb-3">
          The Throughline // 2003-present
        </p>
        <h2 class="text-2xl sm:text-3xl font-bold text-text-primary">
          The philosophy never changed. Only the domain did.
        </h2>
      </div>

      <div class="space-y-0">
        <div class="group flex items-stretch border-l-2 border-edge-green/20 hover:border-edge-green/50 transition-colors">
          <div class="pl-6 py-5 flex-1">
            <div class="flex items-center gap-3 mb-1.5">
              <span class="font-mono text-xs text-edge-green">2003-2007</span>
              <span class="text-text-muted font-mono text-xs">//</span>
              <span class="font-semibold text-text-primary">Toolbox</span>
            </div>
            <p class="text-sm text-text-muted leading-relaxed">
              Node-based procedural graphics. Connect Stripe, Noise, Bump into a DAG &rarr; generate patterns.
            </p>
            <p class="font-mono text-[11px] text-text-muted mt-1">input &rarr; <span class="text-edge-green">[nodes]</span> &rarr; procedural graphics</p>
          </div>
        </div>
        <div class="group flex items-stretch border-l-2 border-rust-orange/20 hover:border-rust-orange/50 transition-colors">
          <div class="pl-6 py-5 flex-1">
            <div class="flex items-center gap-3 mb-1.5">
              <span class="font-mono text-xs text-rust-orange">2018-present</span>
              <span class="text-text-muted font-mono text-xs">//</span>
              <span class="font-semibold text-text-primary">LODA</span>
              <span class="font-mono text-xs text-text-muted">~6,000 commits</span>
            </div>
            <p class="text-sm text-text-muted leading-relaxed">
              Assembly instructions &rarr; integer sequences. Miner discovers novel formulas no human wrote.
            </p>
            <p class="font-mono text-[11px] text-text-muted mt-1">OEIS definition &rarr; <span class="text-rust-orange">[evolutionary search]</span> &rarr; formula</p>
          </div>
        </div>
        <div class="group flex items-stretch border-l-2 border-arc-cyan/20 hover:border-arc-cyan/50 transition-colors">
          <div class="pl-6 py-5 flex-1">
            <div class="flex items-center gap-3 mb-1.5">
              <span class="font-mono text-xs text-arc-cyan">2020-present</span>
              <span class="text-text-muted font-mono text-xs">//</span>
              <span class="font-semibold text-text-primary">ARC-Interactive</span>
              <span class="font-mono text-xs text-text-muted">725 commits</span>
            </div>
            <p class="text-sm text-text-muted leading-relaxed">
              Input grid &rarr; transformation rules &rarr; output grid. 120+ tasks created, community tooling.
            </p>
            <p class="font-mono text-[11px] text-text-muted mt-1">grid &rarr; <span class="text-arc-cyan">[rules]</span> &rarr; transformed grid</p>
          </div>
        </div>
        <div class="group flex items-stretch border-l-2 border-node-blue/20 hover:border-node-blue/50 transition-colors">
          <div class="pl-6 py-5 flex-1">
            <div class="flex items-center gap-3 mb-1.5">
              <span class="font-mono text-xs text-node-blue">2024-present</span>
              <span class="text-text-muted font-mono text-xs">//</span>
              <span class="font-semibold text-text-primary">PlanExe</span>
              <span class="font-mono text-xs text-warn-amber">336 stars</span>
            </div>
            <p class="text-sm text-text-muted leading-relaxed">
              Task descriptions &rarr; dependency graph &rarr; structured execution plan with Gantt, risk, governance.
            </p>
            <p class="font-mono text-[11px] text-text-muted mt-1">prompt &rarr; <span class="text-node-blue">[dependency DAG]</span> &rarr; plan</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════════════════════════════════
       LOBSTER PLAYGROUND — Links to the assistant tribute pages
       Uses lobster-swarm.png as hero image
       ════════════════════════════════════════════════════════════════════ -->
  <section class="border-t border-border bg-bg-secondary py-20 sm:py-24">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-10 items-center">
        <!-- Left: lobster swarm image -->
        <div class="relative rounded-lg overflow-hidden border border-border">
          <img src="/generated/lobster-swarm.png"
               alt="Four lobsters at computer terminals in a dark server room, working on ARC puzzles, code, and planning DAGs"
               class="w-full object-cover" loading="lazy" />
          <div class="absolute top-3 left-3 bg-bg-primary/80 px-2 py-1 rounded">
            <p class="font-mono text-[9px] text-rust-orange">U3 Lobster Swarm // server room</p>
          </div>
        </div>

        <!-- Right: playground links -->
        <div>
          <p class="font-mono text-xs text-rust-orange uppercase tracking-[0.2em] mb-3">
            Lobster Playground
          </p>
          <h2 class="text-2xl sm:text-3xl font-bold text-text-primary mb-4">
            Three assistants, three takes on Simon.
          </h2>
          <p class="text-sm text-text-muted leading-relaxed mb-4">
            The U3 Lobster Swarm — Larry, Egon, and Bubba — each built their own tribute page
            for Simon Strandgaard. Same data, different perspectives. Browse them all.
          </p>
          <p class="text-sm text-text-muted leading-relaxed mb-6">
            Larry and Bubba are operated by
            <a href="https://markbarney.net" target="_blank" rel="noopener" class="text-node-blue hover:underline">Mark Barney</a>;
            Egon is Simon's. Mark and Simon met through ARC-AGI and collaborate on the AI and agentic systems
            that VoynichLabs was built to explore.
          </p>

          <div class="space-y-3">
            <!-- Larry -->
            <a href="/simon-larry"
               class="flex items-center gap-4 bg-bg-terminal border border-border hover:border-edge-green/40 rounded-lg p-4 transition-colors group">
              <div class="w-10 h-10 bg-edge-green/10 border border-edge-green/20 rounded flex items-center justify-center font-mono text-sm text-edge-green flex-shrink-0">
                L
              </div>
              <div class="flex-1 min-w-0">
                <p class="font-semibold text-text-primary group-hover:text-edge-green transition-colors text-sm">Larry's Take</p>
                <p class="text-xs text-text-muted">Narrative storytelling. Origin story, demoscene roots, long-form biography.</p>
              </div>
              <span class="font-mono text-xs text-text-muted group-hover:text-edge-green transition-colors">--></span>
            </a>
            <!-- Egon -->
            <a href="/simon-egon"
               class="flex items-center gap-4 bg-bg-terminal border border-border hover:border-node-blue/40 rounded-lg p-4 transition-colors group">
              <div class="w-10 h-10 bg-node-blue/10 border border-node-blue/20 rounded flex items-center justify-center font-mono text-sm text-node-blue flex-shrink-0">
                E
              </div>
              <div class="flex-1 min-w-0">
                <p class="font-semibold text-text-primary group-hover:text-node-blue transition-colors text-sm">Egon's Take</p>
                <p class="text-xs text-text-muted">Data-dense. Stats tables, repository index, verbatim citations.</p>
              </div>
              <span class="font-mono text-xs text-text-muted group-hover:text-node-blue transition-colors">--></span>
            </a>
            <!-- Bubba -->
            <a href="/simon-bubba"
               class="flex items-center gap-4 bg-bg-terminal border border-border hover:border-rust-orange/40 rounded-lg p-4 transition-colors group">
              <div class="w-10 h-10 bg-rust-orange/10 border border-rust-orange/20 rounded flex items-center justify-center font-mono text-sm text-rust-orange flex-shrink-0">
                B
              </div>
              <div class="flex-1 min-w-0">
                <p class="font-semibold text-text-primary group-hover:text-rust-orange transition-colors text-sm">Bubba's Take</p>
                <p class="text-xs text-text-muted">Visual-forward. Big hero, bold stats, striking project cards.</p>
              </div>
              <span class="font-mono text-xs text-text-muted group-hover:text-rust-orange transition-colors">--></span>
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════════════════════════════════
       CTA — Clean, terminal-style
       ════════════════════════════════════════════════════════════════════ -->
  <section class="border-t border-border py-20">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 text-center">
      <p class="font-mono text-xs text-text-muted mb-6">
        🦞 Simon Strandgaard &middot; neoneye &middot; Copenhagen
      </p>
      <h2 class="text-2xl sm:text-3xl font-bold text-text-primary mb-4">
        199 public repositories. Decades of work.
      </h2>
      <p class="text-sm text-text-muted mb-8">
        Recognized by the ARC Prize Foundation. Co-creator of LODA. Builder of tools that serious researchers depend on.
      </p>
      <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
        <a href="/simon"
           class="px-6 py-2.5 bg-bg-surface border border-node-blue/40 hover:border-node-blue text-node-blue font-mono text-sm rounded transition-colors duration-200">
          cat ./simon
        </a>
        <a href="https://github.com/neoneye" target="_blank" rel="noopener"
           class="px-6 py-2.5 bg-bg-surface border border-border hover:border-border-active text-text-muted hover:text-text-primary font-mono text-sm rounded transition-colors duration-200">
          open github.com/neoneye
        </a>
      </div>
    </div>
  </section>


<style>
  @keyframes dag-node-pulse {
    0%, 100% { opacity: 0.22; }
    50%       { opacity: 0.50; }
  }
  .dag-node-pulse {
    animation: dag-node-pulse var(--pulse-dur, 6s) ease-in-out infinite;
    animation-delay: var(--pulse-delay, 0s);
  }
  @keyframes dag-edge-draw {
    0%   { stroke-dashoffset: 1000; opacity: 0; }
    8%   { opacity: 0.18; }
    100% { stroke-dashoffset: 0; opacity: 0.18; }
  }
  .dag-edge-grow {
    stroke-dasharray: 1000;
    stroke-dashoffset: 1000;
    animation: dag-edge-draw var(--edge-dur, 6s) ease-out forwards;
  }
</style>

<script>
  // DAG hero — slow idle growth. Every position is pre-calculated to extend
  // the existing clusters outward along the viewport margins, away from the
  // hero text (center band ~x:300-900, y:200-550).
  // No randomness. Each entry: [x1, y1, x2, y2, color, r, edgeDurSeconds, delayAfterPrevMs]
  (function () {
    const svg = document.querySelector('.dag-hero-svg');
    if (!svg) return;

    // Staggered pulse on existing static nodes
    svg.querySelectorAll('circle').forEach((c, i) => {
      c.style.setProperty('--pulse-dur', (5 + (i % 5)) + 's');
      c.style.setProperty('--pulse-delay', (i * 0.65) + 's');
      c.classList.add('dag-node-pulse');
    });

    // Pre-designed extensions — each chains naturally from an existing endpoint.
    // Clusters stay in the margins; no new nodes in the hero text zone.
    //   col 0-3: coords  col 4: stroke  col 5: r  col 6: edge draw dur (s)  col 7: wait after prev (ms)
    const steps = [
      //  x1    y1    x2    y2    color      r    dur  wait
      [   80,  120,   55,  270, '#38bdf8', 2.5,  6,  7000],  // left cluster → down left margin
      [  420,  150,  560,   75, '#4ade80', 3.0,  6,  7000],  // top-left cluster → top margin
      [  840,  640,  990,  700, '#f97316', 3.0,  5,  8000],  // bottom cluster → right
      [ 1120,  200, 1170,  370, '#38bdf8', 2.5,  6,  7000],  // top-right cluster → right margin
      [  280,  560,  130,  630, '#22d3ee', 3.0,  5,  8000],  // bottom cluster → left margin
      [  560,   75,  730,   50, '#22d3ee', 2.5,  7,  7000],  // chains from step 1 node → top edge
      [   55,  270,   35,  440, '#38bdf8', 2.0,  6,  8000],  // chains from step 0 node → further down
      [  990,  700, 1110,  670, '#f97316', 2.5,  5,  7000],  // chains from step 2 node
      [  900,  180, 1010,   70, '#22d3ee', 3.0,  6,  8000],  // top-right cluster → top margin
      [  340,  340,  190,  430, '#f97316', 3.0,  5,  7000],  // top-left cluster → left-middle
      [  730,   50,  890,   80, '#4ade80', 2.5,  6,  8000],  // chains from step 5 → further right top
      [ 1170,  370, 1185,  530, '#38bdf8', 2.0,  5,  7000],  // chains from step 3 → right margin
      [  130,  630,   65,  740, '#22d3ee', 2.5,  6,  8000],  // chains from step 4 → bottom-left corner
      [ 1010,   70, 1150,   35, '#38bdf8', 2.0,  5,  7000],  // chains from step 8 → top-right corner
      [  190,  430,   90,  560, '#f97316', 2.5,  6,  8000],  // chains from step 9 → further left
      [  890,   80,  980,   30, '#4ade80', 2.0,  5,  7000],  // chains from step 10 → top edge
      [ 1110,  670, 1180,  760, '#f97316', 2.0,  6,  8000],  // chains from step 7 → bottom-right corner
      [   35,  440,   15,  580, '#38bdf8', 1.5,  7,  9000],  // chains from step 6 → far left edge
    ];

    let idx = 0;
    function revealNext() {
      if (idx >= steps.length) return;
      const [x1, y1, x2, y2, col, r, dur, waitNext] = steps[idx++];

      const line = document.createElementNS('http://www.w3.org/2000/svg', 'line');
      line.setAttribute('x1', x1); line.setAttribute('y1', y1);
      line.setAttribute('x2', x2); line.setAttribute('y2', y2);
      line.setAttribute('stroke', col);
      line.setAttribute('stroke-width', '0.8');
      line.setAttribute('filter', 'url(#edge-glow)');
      line.style.setProperty('--edge-dur', dur + 's');
      line.classList.add('dag-edge-grow');
      svg.appendChild(line);

      // Node appears once the edge finishes drawing
      const circle = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
      circle.setAttribute('cx', x2); circle.setAttribute('cy', y2); circle.setAttribute('r', r);
      circle.setAttribute('fill', col);
      circle.setAttribute('opacity', '0');
      svg.appendChild(circle);
      setTimeout(() => {
        circle.setAttribute('opacity', '0.25');
        circle.style.setProperty('--pulse-dur', (5 + (idx % 5)) + 's');
        circle.style.setProperty('--pulse-delay', (idx * 0.4) + 's');
        circle.classList.add('dag-node-pulse');
      }, dur * 1000);

      setTimeout(revealNext, waitNext);
    }

    // First node appears after 8s of idle
    setTimeout(revealNext, 8000);
  })();
</script>
</Base>
