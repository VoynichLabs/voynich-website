---
// Author: openai-codex/gpt-5.3-codex
// Date: 25 February 2026
// PURPOSE: Lobster Art Museum landing page with featured experimental strip + complete archive.
//          Every piece links to an individual shareable permalink page for social posting.
// SRP/DRY check: Pass - uses shared museum metadata helpers for listing and URL generation.
import Base from '../layouts/Base.astro';
import { getMuseumEntries, getFeaturedEntries, getArchiveEntries } from '../lib/lobster-museum';

const entries = getMuseumEntries();
const featuredEntries = getFeaturedEntries(entries);
const archiveEntries = getArchiveEntries(entries);
const sculptureHighlights = entries.filter((entry) => entry.filename.startsWith('b17-')).length;
---

<Base
  title="Lobster Art Museum"
  description="Lobster Art Museum — dedicated route for generated artwork archive with individual shareable pages for every piece."
  canonicalUrl="/lobster-art-museum"
>
  <main class="max-w-7xl mx-auto px-4 py-16 sm:py-24">
    <section class="mb-12 border border-border rounded-2xl overflow-hidden">
      <div class="bg-gradient-to-br from-bg-secondary via-bg-primary to-bg-secondary p-8 sm:p-12">
        <p class="font-mono text-xs text-rust-orange uppercase tracking-[0.25em] mb-3">
          Lobster Art Museum
        </p>
        <h1 class="text-3xl sm:text-5xl font-extrabold text-text-primary mb-4 leading-tight">
          Freaky, abstract, experimental — now front and center.
        </h1>
        <p class="text-sm sm:text-base text-text-muted max-w-3xl leading-relaxed">
          Every piece in this museum now has its own permalink page for sharing. Featured strip up top, full archive
          below, and no artwork gets lost in the pile.
        </p>
        <div class="mt-6 flex flex-wrap gap-3 text-xs font-mono">
          <span class="px-3 py-1 rounded-full border border-border text-edge-green">{entries.length} total works</span>
          <span class="px-3 py-1 rounded-full border border-border text-node-blue">{featuredEntries.length} featured experimental works</span>
          <span class="px-3 py-1 rounded-full border border-border text-rust-orange">{sculptureHighlights} sculpture-tour pieces</span>
        </div>
        <div class="mt-6 flex flex-wrap gap-3 text-[11px] font-mono uppercase tracking-[0.18em]">
          <a href="/lobster-art-museum/expensive-medieval-tapestry" class="text-rust-orange hover:text-edge-green transition-colors">
            Tapestry spotlight
          </a>
          <a href="/lobster-art-museum/manifest.json" class="text-node-blue hover:text-edge-green transition-colors">
            Caption manifest JSON
          </a>
        </div>
      </div>
    </section>

    {featuredEntries.length > 0 && (
      <section class="mb-12">
        <div class="flex items-center justify-between mb-4 gap-4">
          <h2 class="text-xl sm:text-2xl font-bold text-text-primary">Featured Experimental Strip</h2>
          <p class="text-xs font-mono text-text-muted uppercase tracking-[0.2em]">Shareable piece URLs ready</p>
        </div>
        <div class="flex gap-4 overflow-x-auto pb-3 snap-x snap-mandatory">
          {featuredEntries.map((entry) => (
            <article class="snap-start min-w-[280px] sm:min-w-[340px] max-w-[380px] border border-border rounded-xl overflow-hidden bg-bg-secondary shadow-sm">
              <a href={entry.href} class="block">
                <img src={entry.src} alt={entry.title} loading="lazy" class="w-full h-52 object-cover bg-bg-primary" />
              </a>
              <div class="p-4 border-t border-border">
                <a href={entry.href} class="text-sm font-semibold text-text-primary hover:text-node-blue transition-colors">
                  {entry.title}
                </a>
                <p class="mt-1 text-[10px] uppercase tracking-[0.25em] text-text-muted">{entry.series}</p>
                <div class="mt-3 flex gap-3 text-[10px] uppercase tracking-[0.2em]">
                  <a href={entry.href} class="text-rust-orange hover:text-edge-green transition-colors">Permalink</a>
                  <a href={`/lobster-art-museum/manifest.json`} class="text-node-blue hover:text-edge-green transition-colors">Manifest</a>
                </div>
              </div>
            </article>
          ))}
        </div>
      </section>
    )}

    {entries.length > 0 ? (
      <section>
        <h2 class="text-xl sm:text-2xl font-bold text-text-primary mb-4">Archive Gallery</h2>
        <section class="columns-1 sm:columns-2 lg:columns-3 gap-5 [column-fill:_balance]">
          {archiveEntries.map((entry) => (
            <figure class="mb-5 break-inside-avoid rounded-xl border border-border bg-bg-secondary overflow-hidden shadow-sm">
              <a href={entry.href} class="block">
                <img
                  src={entry.src}
                  alt={entry.title}
                  loading="lazy"
                  class="w-full h-auto object-contain bg-bg-primary"
                />
              </a>
              <figcaption class="px-4 py-3 border-t border-border">
                <a href={entry.href} class="text-sm font-semibold text-text-primary hover:text-node-blue transition-colors">
                  {entry.title}
                </a>
                <div class="mt-1 text-[10px] uppercase tracking-[0.25em] text-text-muted">{entry.series}</div>
                <a
                  href={entry.href}
                  class="inline-flex mt-2 text-[10px] uppercase tracking-[0.2em] text-rust-orange hover:text-edge-green transition-colors"
                >
                  Open share page →
                </a>
              </figcaption>
            </figure>
          ))}
        </section>
      </section>
    ) : (
      <section class="border border-border rounded-xl p-6 bg-bg-secondary">
        <p class="text-sm text-text-muted">No generated artwork found yet in <code>/public/generated</code>.</p>
      </section>
    )}
  </main>
</Base>
